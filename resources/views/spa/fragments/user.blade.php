<div class="spa-fragment">
    <h1 class="text-xl font-semibold capitalize text-zinc-900">{{ str_replace('_', ' ', $page) }}</h1>
    <p class="mt-2 text-sm text-zinc-600">Konten halaman user: <code class="rounded bg-zinc-100 px-1">{{ $page }}</code></p>
</div>
